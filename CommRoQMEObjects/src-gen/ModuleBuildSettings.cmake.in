#--------------------------------------------------------------------------
# Code generated by the SmartSoft MDSD Toolchain
# The SmartSoft Toolchain has been developed by:
#  
# Christian Schlegel (schlegel@hs-ulm.de)
# University of Applied Sciences Ulm
# Prittwitzstr. 10
# 89075 Ulm (Germany)
#
# Information about the SmartSoft MDSD Toolchain is available at:
# www.servicerobotik-ulm.de
#
# Please do not modify this file. It will be re-generated
# running the code generator.
#--------------------------------------------------------------------------

# get crrent path
GET_FILENAME_COMPONENT(CURR_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

IF(EXISTS ${CURR_CMAKE_DIR}/CMakeCache.txt)
  # in build tree
  LIST(APPEND COMMROQMEOBJECTS_INCLUDE_DIRS @PROJECT_SOURCE_DIR@/src)
  LIST(APPEND COMMROQMEOBJECTS_INCLUDE_DIRS @PROJECT_SOURCE_DIR@/src-gen)

  IF(NOT TARGET @PROJECT_NAME@)
    INCLUDE("@PROJECT_BINARY_DIR@/@PROJECT_NAME@Target.cmake")
  ENDIF()
ELSE()
  # installed
  LIST(APPEND COMMROQMEOBJECTS_INCLUDE_DIRS "@CMAKE_INSTALL_PREFIX@/include")

  IF(NOT TARGET @PROJECT_NAME@)
    IF(EXISTS ${CURR_CMAKE_DIR}/@PROJECT_NAME@Target.cmake) 
      INCLUDE("${CURR_CMAKE_DIR}/@PROJECT_NAME@Target.cmake")
    ELSE()
      MESSAGE(WARNING "Could not find @PROJECT_NAME@Target.cmake")
    ENDIF()
  ENDIF()
ENDIF()

# these are the output variables to be used by external packages in their build process
LIST(APPEND COMMROQMEOBJECTS_LIBRARIES CommRoQMEObjects)
SET(COMMROQMEOBJECTS_FOUND TRUE)

# export to parent scope
LIST(APPEND SMART_EXPORT_VARS COMMROQMEOBJECTS_FOUND)
LIST(APPEND SMART_EXPORT_VARS COMMROQMEOBJECTS_INCLUDE_DIRS)
LIST(APPEND SMART_EXPORT_VARS COMMROQMEOBJECTS_LIBRARIES)
